\appendix
\section{模块之间依赖关系}
\begin{figure}[H]
    \centering
    \begin{tikzcd}[font=\Large]
        & \verbbox[green]{color}\arrow[dr, bend left]\arrow[dl, bend right] & \\
        \verbbox[green]{debug}\arrow[dr] & \verbbox[green]{warn}\arrow[d] & \verbbox[violet]{analysis}\arrow[dl,Rightarrow] \\
        \verbbox[violet]{regressor}\arrow[r,Rightarrow] & \verbbox[red]{main} & \verbbox[green]{timer}\arrow[l] \\
        \verbbox[green]{progress}\arrow[ur] & & \verbbox[violet]{plot}\arrow[ul,Rightarrow] \\
        \verbbox[violet]{data}\arrow[uur, bend right,Rightarrow] & & \verbbox[green]{cfgs}\arrow[uul, bend left]\arrow[ll]
    \end{tikzcd}
    \caption{程序间依赖关系}\label{F:code-dependency}
\end{figure}


\section{运行结果数据结构}
\tikz [font=\texttt\footnotesize,
    grow=right, level 1/.style={sibling distance=3em,level distance=.1\textwidth},
                level 2/.style={sibling distance=3em,level distance=.15\textwidth},
                level 3/.style={sibling distance=1.5em,level distance=.2\textwidth},
                level 4/.style={sibling distance=1em,level distance=.25\textwidth},]
    \node{Result}
        child { node{cnt}
            child { node{\dots} }
        }
        child { node{casual}
            child { node{\dots} }
        }
        child { node{registered}
            child { node{info}
                child { node{\vdots} }
                child { node{AdaBoostRegressor}
                    child { node{r2\_score} }
                    child { node{fit\_time} }
                    child { node{pred\_time} }
                }
            }
            child { node{best}
                child { node{model} }
                child { node{r2\_score} }
            }
        };


\section{\texttt{Python}代码附录}
\subsection{第三方库依赖}
\Python{第三方库依赖}{../Code/requirements.txt}

\subsection{主文件}
\Python{主文件}{../Code/main.py}

\subsection[分析模块]{分析模块（数据预处理、数据降维、模型评价）}
\Python{分析模块（数据预处理、数据降维、模型评价）}{../Code/analysis.py}

\subsection[数据模块]{数据模块（训练集与测试集）}
\Python{数据模块}{../Code/data.py}

\subsection{可视化模块}
\Python{可视化模块}{../Code/plot.py}

\subsection{回归模型模块}
\Python{回归模型模块}{../Code/regressor.py}

\subsection{配置文件模块}
\Python{配置文件模块}{../Code/cfgs.py}

\subsection{彩色字符串模块}
\Python{彩色字符串模块}{../Code/color.py}

\subsection{调试模块}
\Python{调试模块}{../Code/debug.py}

\subsection{进度条模块}
\Python{进度条模块}{../Code/progress.py}

\subsection{计时模块}
\Python{计时模块}{../Code/timer.py}

\subsection{消除警告模块}
\Python{消除警告模块}{../Code/warn.py}
